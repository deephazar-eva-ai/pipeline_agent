# Copy to .env and fill in. Never commit .env.
#
# Base URL and bearer token for AgentSwitch. The client POSTs JSON-RPC 2.0 to
# {AGENTSWITCH_MCP_URL}/api/mcp and sends the standard MCP initialize handshake.
AGENTSWITCH_MCP_URL=
AGENTSWITCH_MCP_TOKEN=
AGENTSWITCH_TENANT=suryodaya

# `entity_scoped` is the default and is what the seat 07 credential serves:
# 238 tools with names such as Deal.list / Activity.create. Set `generic` only
# for a credential exposing the 13-tool catalogue in the captured
# agent_tools.json. Leave this unset unless a preflight says otherwise - it
# reports the surface the credential really exposes, and says so when this
# setting disagrees with it.
MCP_TOOL_SURFACE=entity_scoped

# Pluggable model backend for --task loop, as "backend:model".
#   anthropic:claude-opus-5   needs ANTHROPIC_API_KEY (or an `ant auth login`
#                             profile) and `pip install anthropic`
#   ollama:llama3.1           needs a local `ollama serve`; override the host
#                             with OLLAMA_HOST (default http://localhost:11434)
# Only --task loop reads this; canonical and preflight never call a model.
MODEL_NAME=

OUTPUT_DIR=runs
